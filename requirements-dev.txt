# Dependencias de desarrollo/testing. Se instalan solo en CI y en el
# entorno local del desarrollador — nunca dentro de las imágenes Docker
# de producción (ver Dockerfiles, que solo usan requirements.txt).

pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
fakeredis==2.26.2
ruff==0.8.4
mypy==1.13.0

# Requirements de todos los servicios, para que pytest pueda importar
# cualquier `app.*` durante la ejecución de tests locales.
-r services/gateway/requirements.txt
-r services/conversation-engine/requirements.txt
-r services/llm-service/requirements.txt
-r services/whatsapp-service/requirements.txt
-r services/spam-filter/requirements.txt
-r services/crm-service/requirements.txt
-r services/analytics/requirements.txt
-r services/notification/requirements.txt
-r services/identity/requirements.txt
